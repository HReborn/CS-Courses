package InsertionSort;

public class InsertionSort {
	public int[] sort(int[] arr) {
		for (int limiter=0; limiter < arr.length; limiter++) {
			int i = 0;
			int border = arr[limiter];
			while (border < arr[limiter-i]) {
				i++;
			}
			
		}
		
		return null;
	}
}
